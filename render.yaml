services:
  - type: web
    name: 3tech-backend
    env: node
    region: oregon
    rootDir: backend
    buildCommand: npm install && npx prisma db push && npx ts-node prisma/seed.ts && npm run build
    startCommand: npm run start
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        fromService:
          type: web
          name: 3tech-frontend
          envVarKey: RENDER_EXTERNAL_URL

  - type: web
    name: 3tech-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: 3tech-backend
          envVarKey: RENDER_EXTERNAL_URL
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
